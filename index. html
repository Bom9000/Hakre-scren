<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>شاشة هاتف خضراء</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #000;
        }
        .phone-screen {
            width: 375px;
            height: 667px;
            background-color: #00FF00; /* اللون الأخضر */
            border-radius: 20px;
            position: relative;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
        }
        .phone-screen:before {
            content: '';
            position: absolute;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 5px;
            background-color: #333;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div class="phone-screen"></div>
</body>
</html>